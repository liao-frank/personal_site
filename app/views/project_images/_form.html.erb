<%= form_with(model: project_image, local: true) do |form| %>
  <% if project_image.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(project_image.errors.count, "error") %> prohibited this project_image from being saved:</h2>

      <ul>
      <% project_image.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :project_id %>
    <%= form.collection_select :project_id, Project.all, :id, :title %>
  </div>

  <div class="field">
    <%= form.label :caption %>
    <%= form.text_area :caption, id: :project_image_caption %>
  </div>

  <div class="field">
    <%= form.label :img_url %>
    <%= form.text_field :img_url, id: :project_image_img_url %>
  </div>

  <div class="field">
    <%= form.label :order %>
    <%= form.number_field :order, id: :project_image_order %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
